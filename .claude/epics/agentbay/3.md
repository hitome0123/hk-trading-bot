---
name: 智能合约 - AgentEscrow.sol 编写与部署
status: open
created: 2026-02-16T03:09:08Z
updated: 2026-02-16T03:13:35Z
github: https://github.com/hitome0123/agentbay/issues/3
depends_on: [2]
parallel: false
conflicts_with: []
---

# Task: 智能合约 - AgentEscrow.sol 编写与部署

## Description
编写 USDC Escrow 智能合约，实现任务创建、接单、资金释放和退款功能，部署到 Monad Testnet。

## Acceptance Criteria
- [ ] AgentEscrow.sol 合约编写完成
- [ ] 使用 OpenZeppelin ReentrancyGuard 和 Pausable
- [ ] createTask / acceptTask / release / refund 函数实现
- [ ] 合约部署到 Monad Testnet
- [ ] 部署地址和 ABI 导出供前端使用

## Technical Details

### 合约核心函数
```solidity
contract AgentEscrow {
    function createTask(uint256 amount, uint256 deadline) external returns (uint256);
    function acceptTask(uint256 taskId) external;
    function release(uint256 taskId) external onlyPlatform;
    function refund(uint256 taskId) external onlyPlatform;
    function claimExpired(uint256 taskId) external;
}
```

### 部署配置
- Network: Monad Testnet
- RPC: [从 Monad 文档获取]
- USDC: 使用 Mock ERC20 或 Monad 原生 USDC

### 输出文件
- `contracts/AgentEscrow.sol`
- `deployments/monad-testnet.json` (地址 + ABI)

## Dependencies
- [ ] Task 001 完成（项目结构）
- [ ] Monad Testnet RPC 访问
- [ ] 部署钱包私钥（测试用）

## Effort Estimate
- Size: M
- Hours: 3
- Parallel: false (需要项目初始化完成)

## Definition of Done
- [ ] 合约代码通过编译
- [ ] 本地 Hardhat 测试通过
- [ ] Monad Testnet 部署成功
- [ ] 导出 ABI 和地址供前端使用
