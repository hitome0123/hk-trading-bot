---
name: 钱包集成 - wagmi 连接 + 合约交互
status: open
created: 2026-02-16T03:09:08Z
updated: 2026-02-16T03:13:35Z
github: https://github.com/hitome0123/agentbay/issues/6
depends_on: [002, 5]
parallel: false
conflicts_with: []
---

# Task: 钱包集成 - wagmi 连接 + 合约交互

## Description
集成 wagmi 钱包连接，实现前端与 AgentEscrow 合约的交互，包括创建任务、接单和查询状态。

## Acceptance Criteria
- [ ] wagmi Provider 配置完成
- [ ] 钱包连接按钮（支持 MetaMask）
- [ ] Monad Testnet 网络配置
- [ ] createTask 合约调用（USDC approve + createTask）
- [ ] acceptTask 合约调用
- [ ] 任务状态读取

## Technical Details

### wagmi 配置
```typescript
// config/wagmi.ts
import { monadTestnet } from './chains';

export const config = createConfig({
  chains: [monadTestnet],
  transports: {
    [monadTestnet.id]: http(),
  },
});
```

### 合约 Hook
```typescript
// hooks/useAgentEscrow.ts
export function useCreateTask() {
  // 1. approve USDC
  // 2. createTask
}

export function useAcceptTask() {
  // acceptTask
}

export function useTaskStatus(taskId: number) {
  // 读取任务状态
}
```

### 组件集成
```typescript
// components/WalletButton.tsx
// components/CreateTaskButton.tsx
// components/AcceptTaskButton.tsx
```

## Dependencies
- [ ] Task 002 完成（合约部署，有地址和 ABI）
- [ ] Task 004 完成（前端页面结构）

## Effort Estimate
- Size: S
- Hours: 2
- Parallel: false (需要合约和前端都完成)

## Definition of Done
- [ ] 钱包可连接/断开
- [ ] 可发起 createTask 交易
- [ ] 交易确认后页面更新
- [ ] 错误处理（用户拒绝、余额不足等）
